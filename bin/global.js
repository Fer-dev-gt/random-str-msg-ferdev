#!/usr/bin/env node                                     
// Es una instrucción que sirve para indicar que este archivo se ejecutará con Nodejs

let random = require('../src/index.js');                // Realizamos la importación de nuestro archivo "index.js"

random.funnyCommit();                                   // Ejecutamos la función "funnyCommit()"